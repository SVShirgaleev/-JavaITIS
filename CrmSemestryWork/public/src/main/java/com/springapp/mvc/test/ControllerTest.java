package com.springapp.mvc.test;

import org.junit.runner.RunWith;
import org.mockito.runners.MockitoJUnitRunner;

/**
 * Created by salavatshirgaleev on 07.05.16.
 */
@RunWith(MockitoJUnitRunner.class)
public class ControllerTest {
   /* @Test
    public void MainControllerShouldReturnGood(){
        GoodInfo goodInfo = mock(GoodInfo.class);
        List<GoodInfo> list = new ArrayList<GoodInfo>();
        list.add(goodInfo);

        MainController mainController = new MainController();
        ModelMap modelMap = null;
        mainController.renderMain(modelMap );
        Assert.assertEquals(list.get(1),modelMap.get(1));

    }*/
}
